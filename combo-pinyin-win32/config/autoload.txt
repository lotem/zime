#--------------------------------------------------------------
# config/autoload.txt
# 
# 此文件是 宫保拼音 的默認配置文件，
# 它將被置為「選擇佈局」菜單的第一項，並於程序啟動時自動載入。
# 請在這裡設置您經常使用的選項。
#--------------------------------------------------------------

#--------------------------------------------------------------
# 「@title 名稱」用於指定當前配置的名稱。
# 名稱的末尾可以加上「(&A)」這樣的快捷鍵提示，
# 在呼出「選擇佈局」菜單時按下「A」鍵就可以啟用該佈局了。
#--------------------------------------------------------------

#@ title 默認(&A)

#--------------------------------------------------------------
# 「@import 文件名」用於引用另一個配置文件的內容。
# 「文件名」所指定的文件會被插入到「@import」指示符所在位置。
# 「文件名」表示為絕對路徑或與當前文件相對的路徑。
#--------------------------------------------------------------

#@ import 7.txt

